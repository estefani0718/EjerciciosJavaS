// Escribir un programa que calcule el área de un rectángulo:

let usu=parseInt(prompt("ingrese la base del rectangulo"));
let usi=parseInt(prompt("ingrese la altura del rectangulo"));

let area=usu*usi;
alert(`este es el resultado del area del rectangulo; \n${area}`);