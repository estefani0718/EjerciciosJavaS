// . Escribir un programa que calcule la longitud y el área de una circunferencia.


let logitud = 2*Math.PI*"radio";
let area =Math.PI*"radio^2";
let radio=10;
let l=2*Math.PI*radio;
let a =Math.PI*Math.pow(radio,2);

alert(`el resultado de hallar la longitud y el área de una circunferencia es  : \n${l.toFixed(2)}\n${a.toFixed(2)} `);