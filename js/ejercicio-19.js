// Escribir un programa que calcule el área y el volumen de un cilindro.

let r = 3; 
let a = 7;
let area = 2 * Math.PI * radio * (r + a);
let volumen = Math.PI * Math.pow(r, 2) * a;
alert(`el resultado de hallarel área y el volumen de un cilindro es  : \n${area.toFixed(2)}\n${volumen.toFixed(2)} `);