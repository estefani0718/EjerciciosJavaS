// Escribir un programa que muestre por consola la tabla de multiplicar del 1 al 10.

let tabla=parseInt(prompt("ingrese un numero de la tabla de multipicar"));
for(let f=1;f <=10;f++){
    alert(`${tabla}X ${f}=${tabla*f} `)
}